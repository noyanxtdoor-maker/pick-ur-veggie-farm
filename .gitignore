node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Code-knowledge tool output (generated, not source)
.codegraph/
graphify-out/

# Browser-probe scratch artifacts (session-local, not source)
.tmp_capture/
.vercel

# DR restore-drill dumps — real production schema/data snapshots. NEVER commit (contains user rows).
backups/

# Supabase CLI local transient state (project link cache) — machine-local, not source.
supabase/.temp*
